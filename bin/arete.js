#!/usr/bin/env node

/**
 * Areté CLI entry point
 * 
 * For published npm package: runs compiled JS from dist/
 * For dev: use `npm run dev` which runs tsx src/cli.ts directly
 */

import '../dist/cli.js';
