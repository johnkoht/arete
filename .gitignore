# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Note-taking apps
.obsidian/
.trash/
.logseq/
logseq/bak/
.stversions/
*.syncthing*

# Notion exports (if accidentally placed here)
notion-export/

# Environment files (in case someone adds them)
.env
.env.local
.env.*.local

# Local credentials (API keys, tokens, secrets)
# The folder and templates are committed; only actual credential files are ignored
.credentials/credentials.yaml
.credentials/credentials.yml
.credentials/*.json
.credentials/*.key
.credentials/*.pem

# Personal notes that shouldn't be committed
# (uncomment if you want to keep local-only notes)
# personal-notes.md
# scratch-local.md

# User-specific build context (copy from USER.md.example)
USER.md

# QMD cache (if you run qmd in the workspace)
.qmd/

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/

# Node modules
node_modules/
package-lock.json

# TypeScript build output
dist/

# CLI-managed directories (for consumer workspaces)
# Populated by `arete install`; dev: run scripts/dev-setup.sh to seed .agents/skills
.agents/

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Autonomous execution system (runtime files)
dev/autonomous/prd.json
dev/autonomous/progress.txt
