# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Note-taking apps
.obsidian/
.trash/
.logseq/
logseq/bak/
.stversions/
*.syncthing*

# Notion exports (if accidentally placed here)
notion-export/

# Environment files (in case someone adds them)
.env
.env.local
.env.test
.env.*.local

# Local credentials (API keys, tokens, secrets)
# The folder and templates are committed; only actual credential files are ignored
.credentials/credentials.yaml
.credentials/credentials.yml
.credentials/*.json
.credentials/*.key
.credentials/*.pem

# Personal notes that shouldn't be committed
# (uncomment if you want to keep local-only notes)
# personal-notes.md
# scratch-local.md

# User-specific build context (copy from USER.md.example)
USER.md

# QMD cache (if you run qmd in the workspace)
.qmd/

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/

# Node modules
node_modules/
package-lock.json

# TypeScript build output
dist/
*.tsbuildinfo

# CLI-managed / workspace directories (consumer workspaces only)
# Populated by `arete install`; do not commit in the Areté dev repo. For local testing, run `arete install .` or create as needed.
.agents/*
!.agents/skills/
!.agents/skills/**
!.agents/sources/
!.agents/sources/**
.cursor/integrations/
.cursor/tools/
# .cursor/rules/ contains BUILDER-only rules for this dev repo.
# These are NOT copies of runtime/rules/ — they are separate files.
# runtime/rules/ contains GUIDE rules shipped to end users.
context/
now/
goals/
projects/
people/
resources/
templates/
memory/
.arete/
arete.yaml
scratchpad.md
GUIDE.md
# Exception: committed test fixtures
!test-data/
!test-data/**
# Exception: package runtime templates (shipped with CLI) are committed
!packages/runtime/templates/
!packages/runtime/skills/*/templates/
!packages/runtime/skills/*/templates/**
# Exception: packages/runtime/GUIDE.md is the source (shipped with CLI)
!packages/runtime/GUIDE.md

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Deprecated: build assets moved to dev/ (2026-02-09). Remove if present.
.cursor/build/

# Deprecated: skills unified into .agents/skills/ (2026-02-09). Remove if present.
.cursor/skills/
.cursor/skills-core/
.cursor/skills-local/

# Autonomous execution system (runtime files — legacy)
dev/autonomous/prd.json
dev/autonomous/progress.txt

# Per-run execution state (transient — see dev/executions/README.md)
dev/executions/*/
