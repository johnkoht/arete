# Execute consolidate-search-command PRD

## Pi (preferred)

Open the plan in plan mode and use `/build`:

```
/plan open consolidate-search-command
/build
```

## Manual (fallback)

Execute the consolidate-search-command PRD. Load the execute-prd skill from `.pi/skills/execute-prd/SKILL.md`. The PRD is at `dev/work/plans/consolidate-search-command/prd.md` and the task list is at `dev/work/plans/consolidate-search-command/prd.json`. Run the full workflow: pre-mortem → task execution loop → holistic review.
