## Memory Synthesis: Slack-Digest → Topic-Wiki

**Date**: 2026-04-29
**Sources scanned**: 1 high-relevance + 4 keyword-relevant memory entries (out of 150 total), 5 LEARNINGS.md files (core/services, cli/commands, runtime/skills, .pi/skills, .pi/skills/execute-prd), `memory/collaboration.md`, builder profile auto-memory (slack-digest, l3, branch isolation, AI escalation, batch commits)

### Actionable bullets for PRD + execution

1. **Dark-code grep gate must enumerate production callers per export, not just symbol presence** — for this PRD: the rename of `refreshAllFromMeetings → refreshAllFromSources` plus new exports `discoverTopicSources` and the `arete topic list --active --slugs` primitive each need a named non-test caller listed in the AC. Run `rg -n 'discoverTopicSources|refreshAllFromSources' packages/{cli,core,apps}/src` AND `rg -n 'refreshAllFromMeetings' packages/{cli,core,apps}/src` (must return 0 after rename) before declaring any task done. The 8th call site at `packages/apps/backend/src/routes/meetings.ts:244` was missed by plan v1 — the executor must include `packages/apps/` in every grep, not just `cli` + `core`.
   - Source: `memory/entries/2026-04-23_topic-wiki-memory-learnings.md` (sibling build) + `memory/collaboration.md` § "Services tested ≠ services shipped"
   - Why it matters here: The parent topic-wiki-memory build shipped `aliasAndMerge` and `renderActiveTopicsAsSlugList` as tested-but-never-called dark code; the eng-lead caught it post-completion. This PRD adds three new exports and a rename across 8 sites — same shape, same failure mode unless caller enumeration is explicit in each step's AC.

2. **The bias block in slack-digest SKILL.md Phase 2c must be byte-equal to meeting-extraction.ts's active-slug section, enforced by test** — copy the wording verbatim from `packages/core/src/services/meeting-extraction.ts:651` (the active-topic-slug bias block) into `packages/runtime/skills/slack-digest/SKILL.md` Phase 2c. Add a test in `packages/runtime/test/` (or `packages/core/test/runtime/`) that reads both files and asserts byte equality of the bias-block region. Without this test, the skill prompt silently drifts and the dual-tier sprawl defense (prompt bias + Jaccard alias-merge) becomes single-tier.
   - Source: `pre-mortem.md` Risk 1 + `memory/entries/2026-04-23_topic-wiki-memory-learnings.md` "dual-tier sprawl defense"
   - Why it matters here: Skill markdown is not unit-tested; prompt regressions are invisible until topic narratives sprawl. The byte-equality test is the cheap, durable signal. Promote pre-mortem mitigation into Step 4 acceptance — this is the executor's responsibility, not a deferred follow-up.

3. **Drop `parseSlackDigestFile` entirely — empirically refuted by parser shape verification** — `parseMeetingFile` (`packages/core/src/services/meeting-context.ts:163`) tolerates missing `attendees` (defaults `[]`), reads `topics` directly via `Array.isArray(fm.topics)`, and parses real `2026-04-28-slack-digest.md` cleanly. PRD Step 2 must NOT introduce a sibling parser. Add a unit test that parses a real slack-digest fixture through `parseMeetingFile` and asserts `topics`, `date`, body extraction. If this becomes load-bearing later (a frontmatter-shape divergence), call it out then — don't pre-fork the schema.
   - Source: `review.md` Concern #2 + `pre-mortem.md` Risk 2 (both verified empirically against an arete-reserv fixture)
   - Why it matters here: Two parsers consuming the same frontmatter shape forks the schema if either side adds fields. The sibling `slack-evidence-dedup` plan may add commitment-ID frontmatter — a forked parser path doubles the surface where that propagates. Single parser, one fixture test. The plan's own review and pre-mortem agree.

4. **Locking is non-reentrant `O_CREAT|O_EXCL` (NOT symmetric/queueing) — slack-digest skill must catch `SeedLockHeldError` and continue** — `packages/core/src/services/seed-lock.ts:54` uses `fs.open(path, 'wx')`. `meeting approve` already catches and emits `warn(...)` + continues. The slack-digest skill shells out to `arete topic refresh --slugs ... --source ...` — the CLI side must (a) exit non-zero with `{error: 'seed_lock_held'}` JSON shape on collision, (b) emit a `SeedLockHeldError`-friendly warning per the 2026-04-23 LEARNINGS pattern, and (c) the skill markdown in `slack-digest/SKILL.md` Phase 5 must explicitly document "topic refresh deferred — re-run when current operation completes" as a non-fatal outcome, NOT crash mid-skill. Consider adding `topics_integrated: false` to digest frontmatter on lock-skip so a later `arete memory refresh` can detect and recover the partial state.
   - Source: `pre-mortem.md` Risk 3 + `packages/cli/src/commands/LEARNINGS.md` § "SeedLockHeldError is a friendly warning, not a stack trace" (2026-04-23)
   - Why it matters here: The plan v1 risk text claims "symmetric locking is verified" — that's wrong. Lock asymmetry is the exact lesson from the parent build that was caught in round-2 review. Any code path that writes to topics/ from a markdown skill (no native catch+retry) needs this hardening, or first-encounter users will see crashes during their first concurrent meeting+slack workflow.

5. **`--source` flag must actually scope `discoverTopicSources` to that file path — DO NOT ship as a label-only logging hint** — pass `sourcePath?: string` into `refreshAllFromSources` options; when set, filter discovery to entries where `entry.path === sourcePath` (~20 LOC). This mirrors `--skip-topics` and the four-things-LLM-spending-commands-need pattern (dry-run, --yes/threshold gate, ARETE_SEED_MAX_USD ceiling, ARETE_NO_LLM kill switch — all four must be honored on this new flag combo). Without scoping, a workspace with 10 prior digests tagged `cover-whale-templates` runs 10× the user's expected cost on first integration. Idempotency-via-content-hash protects re-runs only, not first runs against pre-tagged content.
   - Source: `review.md` Concern #8 + `pre-mortem.md` Risk 4 + `packages/cli/src/commands/LEARNINGS.md` § "LLM-spending commands need four things" (2026-04-23) + builder feedback `~/.claude/.../feedback_ai_fix_escalation.md` (cheapest path = correct semantics, not deferred docs)
   - Why it matters here: The plan currently defers this to "documentation explicitly." Plan-review and pre-mortem both flag re-litigating it; user's collaboration.md emphasizes "candid engineering judgment" and "don't ship label-only when scoped is 20 LOC." This is the highest-leverage UX correctness decision in the PRD.

### Cited entries

- `memory/entries/2026-04-23_topic-wiki-memory-learnings.md` — Parent build's full as-built; dark-code failure, asymmetric-lock, dual-tier sprawl defense, structured-output guards, body-only hashing — directly load-bearing for every step here.
- `memory/entries/2026-04-10_krisp-oauth-weekly-winddown-alignment.md` — Last skill-drift entry pre-this-plan; reinforces "skill drift is silent" and "dead formats in skills" — relevant to the bias-block byte-equality concern (Bullet 2) and the `topics_integrated: false` recovery flag (Bullet 4).
- `memory/entries/2026-03-02_skill-integration-hooks-learnings.md` — Skill integration mechanics; sentinel markers, idempotency, integration profiles — the SKILL.md Phase 5 changes here follow the same pattern (skill writes intermediate state, CLI verbs do the heavy lifting).
- `packages/core/src/services/LEARNINGS.md` § "Body-only hashing for meeting-source idempotency" + § "Advisory lock at outer CLI boundary; service respects `skipLock`" — both 2026-04-23, both directly used by Steps 2 and 3.
- `packages/cli/src/commands/LEARNINGS.md` § "LLM-spending commands need four things" + § "SeedLockHeldError is a friendly warning" + § "`--skip-topics` escape on both `meeting apply` and `meeting approve`" — Step 3's CLI flags must honor all four patterns.
- `memory/collaboration.md` § "Per-phase reviewer subagent protocol is load-bearing" + § "Services tested ≠ services shipped" + § "Reviewer prompts should be direct" — execution discipline for Phase 4.
- `~/.claude/projects/-Users-john-code-arete/memory/project_slack_digest.md` — slack-digest skill is Phase 3g (daily) / Phase 4d (weekly); MCP is the access layer; runs AFTER meetings — sequencing context Step 3 must preserve.
- `~/.claude/projects/-Users-john-code-arete/memory/feedback_batch_commitments.md` — `--skip-qmd` per call, `arete index` once at end. Step 3's wiring must follow this pattern in the skill (already standard for slack-digest's existing CLI calls).
