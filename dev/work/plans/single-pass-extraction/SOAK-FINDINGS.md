# Soak findings — single_pass + checklist (arete-reserv daily winddown)

Running log of findings from John's live soak. Each: observation → verdict → action.

## Night 2 — 2026-06-16

### Working ✅
- **Render fix took** — agent ran the checklist render, explained `[x]=keep / [ ]=skip` semantics, pre-filled confidently. The grouped-checkbox format is live.
- Judgment correct: kept CT1 open + reframed to "google survey/poll" per John, ran only the resolves John OK'd ("the rest are good"), caught the Doris/Nate week.md cascade and reverted the auto-`[x]` to honest `[ ]`.

### Gaps / fixes
7. **[FIXING] wait-gate failed + conversational execution bypassed `apply`.** Mid-review, John asked a question and gave a partial CT1 edit instruction. The agent treated that as approval and EXECUTED the proposed-actions/CT batch (4 resolves, week.md Tue-progress fill, drafting the DM) — without an explicit "proceed." Two problems: (a) **a clarifying question / partial edit must NOT trip execution** — SKILL.md has a wait-gate ("wait for user response, no writes until received") but it's not robust to partial/question input; (b) **the agent hand-ran `commitments resolve` + week.md edits conversationally instead of routing through `arete winddown apply`** — defeating the single gated commit point (apply's confirm summary) that's the whole purpose of the checkbox flow. It DID correctly hold the staged-items checklist pending confirm. Fix = SKILL.md hardening: questions/edits ≠ approval (answer, absorb edits into the doc, re-wait); only an explicit proceed or `arete winddown apply` executes; drive ALL execution through `apply`, not conversational primitives. No data harm this run (executed actions matched John's intent).

8. **[FIXING] action items render with NO owner/direction; `direction: none` items mistreated as John's to-dos.** Extraction typing is CORRECT (verified 2026-06-16 claim-portal-comms: 6/8 items = `direction: none` (others' actions — Philip/Phil W/Rachael/Bavitha/Conner/Lindsay), 1 = `they_owe_me` Anthony→John). But the checklist renderer (`winddown-checklist.ts`) surfaces tier+text only — no owner, no direction. Worse: `none` items are pre-filled `[x]` in the MAIN "Action items" list, indistinguishable from John's real tasks, burying his actual to-dos among 6 that aren't his. Per D7, `none` = visibility-only, never his commitment → should be a separate "Others' actions (FYI)" group / sidecar, not pre-checked. No data harm (D7 inertness tested — checking a `none` item creates no commitment on apply) but the review is misleading + defeats the winddown's core "what do I owe / what's owed me" purpose. Fix (renderer): (a) show owner+direction per line — `(you → X)` / `(X → you)` / `(X's — FYI)`; (b) split `direction: none` out of the actionable list, un-pre-filled. Pairs with finding #7 (gate-hardening) as one branch pass.

9. **[SEPARATE FOLLOW-UP] meeting importance is dead infrastructure — never populated, NOT a regression.** Every meeting May+June is `importance: normal` (66 June / 59 May, zero variation). The enum (`light`/`normal`/`important`/`skip`), the light-extraction path (lighter prompt + auto-approve, meeting-processing.ts:384-385), and a `skip` value all exist — but nothing classifies or sets them; everything defaults to `normal`. So John's skip-worthy eng standups (claim-portal-comms) get identical full extraction to his most important calls. Pre-dates the branch (not single_pass's fault). **Symptom largely fixed by #8** (claim-portal flood is mostly `direction: none` → moving those out of the actionable list collapses the meeting). **Deeper fix = its own piece:** prefer a per-recurring-meeting importance/skip map (deterministic, hooks into area `recurring_meetings` config) over an LLM classifier — John KNOWS which meetings he skips. Not bundled into the #7/#8 patch.

## Night 3 — 2026-06-16 winddown (first run on the FULLY-FIXED build) — much better, 2 concrete issues

**Wins:** dedup CAUGHT the multi-party week.md dupe (CT3: "Philip/Dave/Bavitha/Lindsay Notion roadmap" → already-tracked `2530e74b` via Rule 4 reconcile — exactly the eval's "downstream reconcile catches it" prediction; John's original 10b complaint, fixed in practice). Re-run idempotency works (2nd pass detected staging already committed → commitments-only reconcile, no re-stage). Uncertain "⚠ your call" block + choice-checkboxes + tiers all render. Baseline persisted. Overall coherent + high-signal — night-and-day vs Night 2.

**Finding #12 — `staged_item_owner` NOT written this run → owner/direction render (#8) shows NOTHING.** The render code is correct (ownerTag + FYI split, `winddown-checklist.ts:210-318`) and SKILL tells the chef to run `arete winddown render --stdout` (1139-1144) — but the meeting frontmatter has `staged_item_status`/`_importance`/`_uncertain` and **NO `staged_item_owner`** (Night 2 HAD it). So render had no direction data → direction:none items (claim-portal Show-Phil-W/Undraft-PR/etc.) sit in the main Action-items list, unlabeled, no "Others' actions (FYI)" split. The eval's `directionNotOwedBy` check passed because it reads the extraction RESULT, not the persisted frontmatter — so the gap is the **--stage WRITE path not persisting staged_item_owner** in the fixed build (regression vs Night 2). This is why John's "who owns each item" ask still isn't reflected.

> **✅ RESOLVED 2026-06-17. DIAGNOSIS ANSWER: NOT "no-direction-assigned" and NOT "stage-write-drop" — it was the APPROVE/COMMIT step stripping `staged_item_owner` (asymmetrically vs the importance/uncertain maps) + the render fix not yet deployed to arete-reserv's dist.**
> - Model DID assign direction (single_pass defaults every action item to a valid direction; `none` on missing/invalid — `meeting-extraction.ts:1949-1985`). `--stage` DID persist owner (PROVEN: claim-portal `## Approved Action Items` carries inline `(@philip-sheperd ·)` / `(@anthony-avina ← @john-koht)`, which `commitApprovedItems`→`formatActionItemWithOwner` only produces by READING `staged_item_owner`).
> - **Root cause:** `commitApprovedItems` (`staged-items.ts:784-806`) filtered `_owner`/`_status`/`_source`/`_confidence`/`_skip_reason` by `approvedIds` (deleting the key when all approved) but NOT `_importance`/`_uncertain`/`_links`. claim-portal approved all 8 AIs → owner key gone, importance/uncertain orphaned. john-phil-shadow's 2 SKIPPED items kept their owner entries. Finding #12 inspected the post-approve file → saw the strip, mislabeled it stage-write.
> - **Fix:** (1) commit step now filters all 9 sibling maps consistently. (2) single_pass `--stage` writes `staged_item_owner: undefined` when empty (clear-on-re-extract; legacy byte-identical). (3) render-shows-nothing was the dist not having the #8 owner-render — resolved by re-sync. +1 consistency test.

**Finding #13 — Anthony 1:1 extracted 0 AGAIN in the live path (eval got 11) — but now SURFACED + recovered, not silent.** The chef flagged "⚠ Extractor gap: Anthony extracted 0 items" and recovered the outcomes manually (Uncertain + Proposed actions). Root differs from eval: live path merges the agenda + auto-loads priorItems (W4) = Anthony's MANY open commitments; the chef's read is the model "deduped against Anthony's many open commitments and returned nothing." **Likely W4 over-suppression** for recurring 1:1s heavy with open commitments — the "mark don't skip" framing collapses to "nothing new → emit nothing." The eval missed it (ran raw transcript without the live priorItems/agenda context). Concerning: W4 may have introduced a NEW empty-extraction path for recurring meetings. Surfacing is the win (#11 fail-loud-ish awareness); the recurrence is the bug.

> **✅ RESOLVED 2026-06-17. DIAGNOSIS ANSWER: W4 volume is NOT the trigger (it's already capped at 10/category + 4000 chars — `meeting-extraction.ts:1211/1215`); the gaps were (a) no top-level prompt rule against "all-empty because all-known", and (b) the residual silent-empty channel (#11) — a SUCCESSFUL call that PARSES to empty returned a quiet 0 with no failureReason.**
> - **Fix (1) prompt:** judgment rule 9 added to `buildSinglePassExtractionPrompt` — "Never return all-empty for a transcript with content; 'already known' is NOT a reason to emit nothing; emit WITH continuation_of/supersedes." Co-exists with the (unchanged) known-items mark-don't-skip block, so W4's Philip/Dave dedup catch is preserved.
> - **Fix (2) guard (fail-loud):** new `EmptyExtractionError` — `extractMeetingIntelligence` (single_pass only) throws when transcript ≥ `EMPTY_EXTRACTION_MIN_TRANSCRIPT_CHARS` (500) yet intelligence is fully empty (no items/decisions/learnings/open-questions + blank summary+core). New `ExtractionFailureReason: 'empty_extraction'`; CLI catch → failure snapshot + non-zero exit. The Anthony case (27.9k chars, `{}`) now SURFACES instead of a quiet 0. Genuinely-empty short meetings (<500 chars) and any meeting with a summary or any item are untouched (D5). Legacy never throws (byte-identity). priorItems cap UNCHANGED. +7 tests.

## ✅ W7 production-path eval — GATE PASSED 5/5 (2026-06-17, --full real-spend)
Ran `eval-extraction-prodpath-2026-06.ts --full` over the 5-meeting corpus, both modes, real Opus. **SCORECARD 5/5.** Verified through the REAL CLI path (settles the postmortem debt):
- Compliance 6/9: 25 items, `license` blocker recovered (legacy capped it), liability/subro/quality. contextUsed:true.
- Anthony 6/9: 12 items, recipient/Kafka.
- Nate 6/8: 9 items, **direction NOT "John owes Nate" (W3/AC4 fixed)**.
- Anthony 6/16 (empty-case regression): **11 items — silent-empty FIXED** (the meeting that started this thread).
- Phil/John 6/16: 18 items — **PASS but WARN on dedup**: the multi-party "Philip" commitment is present WITHOUT a `continuation_of` marker. Context IS flowing (existingTasks=true, contextUsed:true) but the model isn't reliably marking the dupe. WARN not FAIL (dedup may happen via existingTasks suppression or downstream reconcile). **This is the residual soft spot = John's original 10b complaint.**
- Note: `topicWiki=false` on compliance 6/9 (true elsewhere) → confirms the topic-refresh-staleness gap (separate follow-up), NOT a W2 regression (W2 carries wiki when it exists).

**Cleared to re-enter soak** (already on single_pass + reconcile_shadow:true). WATCH in soak: (1) dedup — does a known-tracked item get flagged/collapsed or re-staged fresh? if re-staged → targeted W4/W5 follow-up (continuation_of not firing despite context present); (2) whitespace-empty channel (finding #11).

## Fix build — APPROVED (2026-06-16, branch fbef437e)
Independent review verdict: approve-with-fixes (fixes all cosmetic). Verified: S1 failure-snapshot fires in the CLI catch before exit (gated on `reconcile_shadow:true`); retry scoped to transport class only (never parse/truncation/auth/deliberate-empty); W2 deserialize shape-guards degrade malformed `--context` instead of crashing fail-loud; W4 auto-load excludePath correct; legacy SUCCESS path byte-identical (regression test passes). 4964 tests green.

**Legacy-error decision: KEEP the throw UNGATED** (call errors surface in legacy too, not just single_pass). Success path is byte-identical; gating to single_pass would re-introduce the silent-empty bug in the fallback mode John runs during rollback. Both legacy callers surface it cleanly. (Confirmed by review + lead.)

**Residual silent-empty channel (finding #11, WATCH):** a whitespace/blank model response in single_pass still returns empty via `meeting-extraction.ts:1773` (the D5-sanctioned "deliberate empty") and records NO `failureReason`. So a 0-item meeting could be a real empty OR a blank response — the one path fail-loud doesn't cover by design. During soak: if any meeting reports 0 items, check the raw snapshot exists and whether it's empty-success vs missing-reason.

**Flip preconditions (before `extraction_mode: single_pass` + re-soak):** (1) run `tsx scripts/eval-extraction-prodpath-2026-06.ts --workspace ~/code/arete-reserv --full` and require SCORECARD N/N (real spend, John's gate — do NOT flip on unit-green alone); (2) ensure `reconcile_shadow: true` in arete.yaml (the S1 diagnosability win is gated on it). Cosmetic follow-ups: stale comment + dead re-throw branch `agent.ts:204/252-253`; the pre-existing double-prefix reconcile bug `meeting.ts:1558/1769`.

## ⛔ Night 2 CRITICAL — single_pass underperforms the old per-meeting flow in PRODUCTION (2026-06-16)

John's verdict: "feels worse than the individual subagents processing each meeting... the agent had almost no context." Evidence — all consistent with a CONTEXT-STARVED production extraction path:

10a. **SILENT-EMPTY EXTRACTION (severity: high).** Anthony 6/16 1:1 (368-line transcript, John made commitments) → single_pass returned `{summary:"", actionItems:[], decisions:[], learnings:[]}`. Confirmed in the raw pre-reconcile snapshot (so NOT reconcile stripping — the extraction call itself produced empty, and failed SILENTLY, reported only as "0 items"). Total extraction loss with no error surfaced.
10b. **No dedup vs open commitments.** "Set up time with Philip/Dave/Bavitha/Lindsay to map Notion roadmap" staged + pre-approved, but it's ALREADY a commitment + in week.md. Context didn't include open commitments → couldn't catch the dupe.
10c. **Wrong direction/relationship.** Proposed DM to Nate "you still owe the mockup share" — Nate DESIGNED it. No person/attendee context → wrong ownership.
10d. **Pre-`[x]` everything amplifies thin output** — pre-fill is only safe if extraction is good; here it makes context-poor output look authoritative.

**ROOT META-LESSON: the benchmark over-promised.** It was run by handing transcripts + context directly to sub-agents — it NEVER exercised the real `arete meeting extract` production path (actual context bundle, prompt/parse, silent-empty failure mode). The old per-meeting flow built attendee/goals/agenda context (`agent.ts` "Builds meeting context for enhanced extraction"); single_pass-via-winddown isn't getting equivalent context to the LLM.

**ACTION: roll soak back to baseline (legacy/prose) — daily driver must work.** Re-plan single_pass: (1) root-cause silent-empty extraction; (2) wire + VERIFY rich context (open commitments for dedup, attendee/person for direction, prior meetings) through the real CLI path; (3) build a real eval that runs the PRODUCTION extract path against ground-truth meetings, not an agent-handoff. The schema/tiers/checkbox-approval/reconcile-fixes work is still sound — the gap is the extraction context path + a benchmark that didn't test it.

## Night 1 — 2026-06-15 (winddown-2026-06-15.md)

Config: extraction_mode=single_pass, reconcile_mode=inline, winddown_render=checklist, reconcile_shadow=true.

### Working ✅
- **single_pass extraction live, output strong.** Frontmatter has `staged_item_importance`; doc shows tiers/P-priorities, `[P4 ⚠ URGENT]` blocker-ish, "Uncertain — your call", continuation markers, ~22 uncapped decisions/learnings across 6 meetings, caught "Carolyn→Caroline" name fix. Core thesis validated on a real day.
- **reconcile_shadow** wrote 6 raw-extraction snapshots to `dev/diary/raw-extractions/`.
- **Reconcile CT1/CT2 collapses** — John confirmed "good catch" (Caroline meeting already scheduled / Dave intro thread fulfilled, with calendar+Slack evidence).
- **Web UI shows NO pre-approved items.** This is CORRECT/intended: single_pass uses tier-derived auto-approval (only `blocker` auto-approves), NOT legacy `confidence>0.8` mass auto-approval (the AC11 fix). No blocker-tier items today → nothing pre-selected → you review everything. Working as designed; aligns with the commitment-rot concern.

### Gaps / fixes
1. **[FIXING] checklist render fell back to prose; no `.baseline.md` written.** Root cause: SKILL.md says "call core renderer `renderWinddownDoc(view)`" — a library fn with no CLI/agent-invokable surface, so the agent had no executable path and fell back to prose + `arete meeting approve`. → Build `arete winddown render <date>` (wraps the tested renderer, writes doc + baseline) + change SKILL.md to call the CLI. **In progress.**
2. **[TWEAK] P4 proposed action was a Slack DM to coordinate scheduling, not a calendar invite.** For "schedule the X call" intents, the proposed-action verb should prefer `calendar.create_event` (propose the actual invite) over a messaging draft. Action-proposal verb selection tweak.
3. **[STRETCH] auto-ingest shared artifacts.** Uncertain item "review Jamie's HTML export + Jordan's visual maps" — John wants the agent to actually DOWNLOAD the shared materials (Slack/Notion links) into the inbox or the correct project, not just stage a review task. Artifact-ingestion enhancement; related to the plan-context / "automagic research" theme. Stretch.
4. **[FIXED-BY-#1] decisions/learnings are a flat list → hard to review.** John's exact pain. The checklist render groups them per-meeting with checkboxes — #1 resolves this.
6. **[FIXING] baseline is incomplete → `apply` would silently DROP every proposed-action.** Found in independent review of the render build (commit a62742dd), before it hit a live run. `render --write` persists ONLY the staged-items block as the baseline. But the agent hand-composes `## Proposed actions` (DMs/invites/resolves) with `<!-- act:verb:id -->` anchors directly in the doc — NOT in the baseline. `winddown-apply.ts:300-303`: a doc anchor with no baseline match → `warnings.push("unknown anchor not in baseline")` + `continue` → the action is dropped, never executed. SKILL.md even contradicts itself (line 1162 "actions carry anchors so apply classifies them" vs 1375 "baseline = staged block is sufficient"). The render build's test only round-tripped render output against itself, so it missed the real flow (agent adds actions the baseline lacks). **This forces the fork answer:** the baseline MUST contain ALL anchored lines (items + choices + actions). Fix = agent persists the COMPLETE composed doc as the baseline (apply ignores non-anchored narrative), not the partial render output. Fixing overnight + adding the missing test (full doc w/ actions → apply → actions classified, zero unknown-anchor warnings).

5. **[MOTIVATES checkbox flow] web UI save-and-approve is SLOW — "part of the problem with this process."** Per-item click→save→approve round-trips. This is the core ergonomic the markdown checkbox + `arete winddown apply` flow eliminates: edit all boxes in the editor at once, one `apply`, done — zero per-item UI round-trips. Reinforces that render/apply is the primary approval path, not the UI.
