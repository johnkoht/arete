# PRD — phase-14-project-write-back

> Generated by the ship suborchestrator (Phase 2.2) from the approved plan + delta pre-mortem. The plan's AC table is the contract; this PRD is the task decomposition. `prd.json` is the machine twin.

## Goal

Close the read-in/write-back loop: `/update-project` proposes README edits from the deterministic phase-12/13 scan on the daily-winddown "proposed" surface (apply exactly what John approves); topics-cache persistence ships as the change-gated `arete project refresh-topics` verb (R1/R2 in tested code); closed projects leave an items/-mediated retro trace (AC5, stretch, mechanism adapted per pre-mortem D5).

## Memory synthesis (Phase 2.1 — binding on task execution)

1. **Never foreground `npm test`** — the suite runs ~64 min and the 600s stream watchdog kills the agent (phase-13 post-mortem). Detach to `/tmp/p14-suite.log`, poll.
2. **Zero-write contracts need two layers**: counting StorageAdapter subclass (no write CALL) + snapshotTree byte-hash (end-to-end CLI) — services/LEARNINGS 2026-06-10. Apply both to refresh-topics preview AND the no-change `--apply` rerun.
3. **CLI verb conventions** (cli/LEARNINGS): `--json` complete in ALL exit paths; `--skip-qmd` + `loadConfig` + `refreshQmdIndex` BEFORE the JSON return, only on actual writes; `findRoot` guard; formatters.ts helpers; routability check at wrap.
4. **Phantom-task check** before every task (ls/grep proposed outputs first); explicit read-first file lists; pre-mortem mitigations embedded in the task itself (collaboration.md — 0 risks materialized across 15+ PRDs when embedded).
5. **Dark-code audit is part of done** — every new export needs a non-test production call site (collaboration.md; bit twice in topic-wiki-memory).
6. **Memory-item live format is `## Title` + `- **Key**:` bullets** parsed by `parseMemorySections`/`parseMemoryItemEntries` with strict priority order (services/LEARNINGS 2026-04-27) — the AC5 retro entry must match it exactly; the format-contract test guards this.
7. **Phase-12 zero-write test files are byte-frozen** (`packages/core/test/services/project-area.test.ts`, `packages/cli/test/commands/project.test.ts`) — all new tests live in new files.

## Tasks

| # | Slice | Task | Key constraints |
|---|---|---|---|
| 1 | 0 | AC6 PATTERNS.md `propose-edits-back-to-source-doc` entry | OWN commit, BEFORE any skill prose (MC4). Documents proposed-surface shape, per-item approval, source attribution, reject-leaves-untouched, R2 change-gated-persistence corollary, named instances. |
| 2 | 1 | AC2 core: `project-topics.ts` (compute/diff/write) + `WikiMatch.score` + `Project.topics?` read-model | Floor = absolute retrieveWiki score (D6: wide margins on both backend scales); change-gate compares slug SETS, zero write calls on same set; ownership comment inserted once, dedup by stable substring (D7); lossless YAML round-trip (nested `jira:`/`notion:`); cap 5. Unit tests in NEW file `project-topics.test.ts`. |
| 3 | 1 | AC2 CLI: `arete project refresh-topics <slug>` | Preview default (computed top-5 + current set + `changed`); `--apply` change-gated end-to-end; `--json` all exits; `--skip-qmd`; qmd refresh only on actual write. Subprocess tests in NEW file incl. preview snapshotTree byte-identical AND the AC8 gate (apply-twice → byte-identical after first, zero writes on rerun). |
| 4 | 1 | AC2 floor calibration on the 23 W4 landing-pad topics (arete-reserv, READ-ONLY) | Score the phase-12 project wiki query for live projects against the landing pads; pick + document the absolute threshold; record table in build-report. No writes outside /tmp. |
| 5 | 2 | AC1: `packages/runtime/skills/update-project/SKILL.md` + `/project` pointer line | Winddown proposed surface; typed menu (status update, decision/learning, open question, meeting link, topics refresh via verb, commitment claim); source attribution; June-fixation worked example verbatim ("propose the goal-date correction; touch nothing else"); backfill-provenance hint (D2); empty-scan mtime-date + day-granularity note (D3); conversational entry resolves meeting first; PATTERNS reference; rollback. Prose tests appended (new describes) in chef-orchestrator-skills.test.ts. |
| 6 | 2 | AC3: `june-fixation` named integration test (NEW file) | Fixture: README Background/Status says "end of June 2026", area-tagged meeting ≥1 day after README mtime says EOY-2026. Asserts: (a) meeting in `whatsNew.meetings`; (b) decision text readable at surfaced path; (c) stale goal line in brief project-context; (d) ZERO writes during scan (counting adapter + snapshotTree). |
| 7 | 2 | AC4: regression wall + R10 guard | `git diff` proves phase-12 test files untouched; phase-12 zero-write suites green; `/project` read-only prose intact; R10 guard test (no brief section/behavior branches on `Project.topics`); README-writers grep recorded verbatim in build-report. |
| 8 | 3 | AC5 (STRETCH): finalize-project retro step + format-contract test | Mechanism per pre-mortem D5 adaptation: append `## Closed project: <name>` entry (exact plan data-model shape) to items/decisions.md; idempotency scan first; then `arete memory refresh`; report where it surfaces. Substrate test: fixture retro entry surfaces in area/project brief Decisions & learnings via existing machinery. |
| 9 | wrap | AC7/AC8/AC9: full suite (detached), gate capture, build-report, rollback.md, docs (cli-commands.md, capabilities.json, routability), memory entry, LEARNINGS, dist rebuild | Stop after ship report — prime runs merge gate. |

## Out of scope

Proactive ambient loop; timestamp-granularity whatsNew (OQ5 parked); `areas:` plural; publish→close-commitment; any write to /Users/john/code/arete-reserv.
