# PRD — Phase 13: Area edge completion (meetings + read-side polish)

> Generated by ship Phase 2.2 from the approved plan + review + delta pre-mortem.
> Plan: `plan.md` (status: approved 2026-06-10). Pre-mortem: `pre-mortem.md` (delta — D1–D6, no CRITICAL).
> Build order: Slice A (tasks 1–6, read-side, gated) → Slice B (tasks 7–12, meeting-area writes) → Slice C (tasks 13–14, claim verb) → Wrap (task 15).

## Goal

Make `area:` a real first-class edge on meetings (the third entity, after projects and commitments): fix the topics-union leak/miss in `meetingsForArea`, add propose-at-process + `set-area` + `backfill-area` write surfaces under the inherited backfill contract, ship the commitment claim verb, and finish the project-brief read surface (siblings-from-area, jira read-side, formatter polish, skill prose) so Phase 14's write-back flow scans clean inputs.

## Memory synthesis (Phase 2.1 — embedded in every task)

1. **Dark-code is the recurring failure mode** — every new export needs a production call-site; audit is a distinct gate (Phase 4.3).
2. **Full `npm test` at wrap, not just touched files** (services LEARNINGS 2026-06-08; per-file `tsx --test` is blind to cross-file conflicts).
3. **No direct `fs` in services; memory-op tests use real fs + StorageAdapter; unique temp dirs** (`pid+Date.now()`).
4. **CLI conventions are load-bearing**: `findRoot()` guard, `--json` complete in ALL exit paths, formatters.ts helpers, register in `index.ts`, qmd refresh needs `loadConfig` + `--skip-qmd`, new verbs must be routable + documented in `cli-commands.md` + AGENTS.md rebuild.
5. **Zero-write guarantees assert BOTH layers**: counting StorageAdapter subclass AND workspace snapshotTree (phase-12 pattern, `project-area.test.ts` + `cli/test/commands/project.test.ts`).
6. **Pre-mortem mitigations embedded per task; sequential execution; per-task commits; commit dist/ at wrap.**

## Tasks

### Slice A — read-side (zero behavioral risk live)

**task-1 — AC1 + AC10 fixtures: `meetingsForArea` prefers explicit `area:`**
`packages/core/src/services/brief-assemblers.ts:238` — change filter to `m.area ? m.area === areaSlug : m.topics.includes(areaSlug)`. All three call sites (`:1202`, `:1381`, `:1626`) inherit via the single function. Update the function's doc comment (W6 union → per-meeting preference, phase-13 AC1).
- AC: leak fixture — meeting `area: pm-operations` + `topics: [glance-2-mvp]` EXCLUDED from glance-2-mvp, INCLUDED for pm-operations (named test `area-edge leak`).
- AC: miss fixture — meeting `area: pm-operations`, no twin topic page → INCLUDED for pm-operations (named test `area-edge miss`).
- AC: accepted-trade-off fixture (review finding 1) — meeting `area: X` + `topics: [Y]` asserted EXCLUDED from Y, test name documents the deliberate R4-bounded recall cost.
- AC: no-area meeting → topics fallback unchanged; existing W6.2 `meetingsForArea` tests pass UNMODIFIED.
- Quality gate: typecheck + targeted `tsx --test`.

**task-2 — AC8: formatter polish (punch #7–9)**
(7) `brief-assemblers.ts` status excerpt (`:1175-1184`): `### YYYY-MM-DD` heading-only chunks in `## Status Updates` not emitted as content; date heading becomes `**[YYYY-MM-DD]**` prefix on its following paragraph. (8) `brief-formatters.ts` `renderSection` (`:31-33`): bullets starting with whitespace pass through WITHOUT the `- ` prefix (kills `-   - …` double-nest). (9) `loadMeetingIndex` excerpt (`:188-197`): skip HTML-comment lines (`<!-- … -->`) when picking the first non-empty line.
- AC: status fixture with dated headings renders dated prefix, no raw `###` in output.
- AC: nested open-work bullets render `  - x` under `- **I owe (1):**`.
- AC: meeting body starting `<!-- merged from … -->` yields the next real line as excerpt.
- AC: person/area/meeting brief formatter outputs unchanged for non-indented bullets (regression).

**task-3 — AC7: `jira:` frontmatter read-side surfacing**
`brief-assemblers.ts` `listActiveProjects`/`readProjectBySlug`: parse `fm.jira` (object → string map; array values comma-joined; non-object ignored, never throw) into `ActiveProject.jira`. Add `jira?: Record<string,string>` to `ProjectBrief['metadata']` (`models/intelligence.ts`) and the `ActiveProject` model. `brief-formatters.ts` `formatProjectBriefMarkdown`: render one `**Jira:** key: VALUE · key: VALUE` line when present.
- AC: object parsed; array values joined; missing/malformed → absent (no line rendered).
- AC: formatter renders the line for a `jira: {idea: GL-12}` fixture (task-management-v1 shape).
- AC: no write path, no validation of ticket IDs.

**task-4 — AC4: siblings from shared `area:` ∪ README links + archive prefix fix**
`brief-assemblers.ts` section-6 block (`:1298-1323`): union = active projects with `p.area === project.area` (excluding self) FIRST, then link-derived extras (`parseSiblingSlugs`) not already present; dedup by slug; archived link-targets labeled; archive lookup tolerates BOTH `archive/<slug>/` and `archive/YYYY-MM_<slug>/` (what `finalize-project` actually writes).
- AC: area-siblings appear with zero README links (task-management-v1 case).
- AC: link-only sibling (cross-area) still appears; self excluded; dedup when both sources yield same slug.
- AC: archived `YYYY-MM_` dir resolves + labeled `_(archived)_`.
- AC: integration — fixture workspace with 3 same-area projects → each brief lists the other 2.

**task-5 — AC6: `/project` skill prose (always-show + broadened triggers)**
`packages/runtime/skills/project/SKILL.md`: presentation step ALWAYS renders Sibling-projects and Related-wiki-pages sections when present in CLI output; when absent, one line ("No siblings / no wiki pages matched"). Add triggers: `load project`, `load the project`, `review project`, `look at (the) project`, `load up`.
- AC: prose-pin tests in `chef-orchestrator-skills.test.ts` assert the always-show rule text + broadened trigger list (honest label: string-presence, soak-verified).

**task-6 — SLICE A GATE: fixture gate + live read-only shadow (MC3a)**
Verify named `area-edge leak`/`area-edge miss` tests green. Run read-only brief section-count shadow over ALL live arete-reserv projects (before = git-stash-free main-built CLI or recorded phase-12 baseline; after = worktree build). Per pre-mortem D6 the population is meaningful (96 area-carrying + 47 topics-arm meetings).
- AC: live brief section counts UNCHANGED for every live project; `git status --porcelain` in arete-reserv byte-identical before/after (zero writes).
- AC: gate numbers written to build-diary. If any count changes → STOP, do not fund Slice B.

### Slice B — meeting-area write surface (overrun-watch: 1.5× tripwire per plan)

**task-7 — D1 mitigation: signal provenance on `AreaMatch`**
`packages/core/src/services/area-parser.ts`: additive `signal?: 'recurring-title' | 'area-name-title' | 'area-name-summary' | 'keyword'` and `corroborated?: boolean` on `AreaMatch` (set per push site; name-match distinguishes title vs summary hit; winner corroborated when a second distinct signal matched the same area). NO behavior change: areaSlug/confidence/matchType identical.
- AC: unit tests for each signal value + corroboration; existing area-parser/consumer suites pass UNMODIFIED.

**task-8 — AC3 core: `meeting-area.ts` helpers**
NEW `packages/core/src/services/meeting-area.ts` (mirrors `project-area.ts`): `listMeetingsForBackfill` (candidates = meetings WITHOUT `area:`; carries title/summary/body inference text + topics + `alsoMatchesViaTopics` (other area slugs in topics, pre-mortem D2) + date for `--days`); `applyAreaToMeeting` via `writeWithLock` with `mtimeGuardSeconds: 0` (pre-mortem D4) + same-values no-op suppression (existing `area`+`area_set_by` equal → zero write calls, review finding 2); `resetBackfilledMeetingAreas` clears ONLY `area_set_by: backfill`; per-match-type qualification helper (pre-mortem D1: `area-name-summary`-only → unqualified; `area-name-title` uncorroborated → qualified but `nameOnly: true`).
- AC: candidate listing skips meetings with `area:` (incl. the 96-style legacy carriers); inference-text assembly = title + summary + body.
- AC: apply writes `area:` + `area_set_by:` preserving body bytes + other frontmatter keys (incl. nested maps); real fs + StorageAdapter, unique temp dirs.
- AC: same-values rerun → ZERO write calls (counting adapter).
- AC: reset scoped to `backfill`; absent-provenance and `approval`/`manual` files untouched.
- AC: apply succeeds on a file written milliseconds earlier (D4 regression).

**task-9 — AC3 CLI: `arete meeting backfill-area`**
`packages/cli/src/commands/meeting.ts`: new subcommand mirroring `project backfill-area`: preview default; `--apply`; `--reset`; `--days N` optional limiter (default all history, OQ1); `--skip-qmd`; `--json` complete in all exit paths; qmd refresh after apply (`loadConfig` present); 0.7 floor + task-8 qualification policy; preview flags `name-only` rows (grouped last + summary line) and shows `also-matches-via-topics` column; surfaces `written: false` results as warnings + `unwritten` count in JSON (D4).
- AC: CLI subprocess tests — preview/apply/reset/days/json paths (pattern: `cli/test/commands/project.test.ts` runCli temp workspace).
- AC: preview leaves fixture workspace byte-identical (snapshotTree).
- AC: name-only flagging + summary line visible in human output; `signal`/`corroborated`/`alsoMatchesViaTopics` in JSON proposals.

**task-10 — AC2a: `arete meeting set-area`**
`meeting.ts`: new subcommand `set-area <file> <area-slug>` with `--set-by approval|manual` (default `approval`, OQ2) + `--json`. Area slug validated against `areas/*.md` (unknown → error, NO write). Write via `writeWithLock` `mtimeGuardSeconds: 0`; surface `written: false` as error (D4).
- AC: round-trip preserves body bytes + other frontmatter keys incl. nested maps.
- AC: unknown area slug → error, no write; `--json` in every exit path.
- AC: set-area immediately after another write succeeds (D4 regression).

**task-11 — AC2b: proposal at `meeting process` + inheritance integration**
`meeting.ts` process action: when the meeting lacks `area:`, run `suggestAreaForMeeting({title, summary, transcript})`; ≥0.7 → `proposedArea: {slug, confidence, signal, corroborated}` in JSON + one human line; <0.7 → `proposedArea: null` in JSON, silent in human output. Process performs ZERO area writes.
- AC: proposal present ≥0.7 / absent <0.7 (unit/subprocess).
- AC: process writes no `area:` key (counting adapter or before/after frontmatter assert).
- AC: integration — process → set-area → approve on a fixture meeting → created commitments carry the area (`meeting.ts:1736` inheritance).

**task-12 — AC2c: process-meetings skill prose**
`packages/runtime/skills/process-meetings/SKILL.md`: present the per-meeting proposal (now sourced from `arete meeting process` output rather than ad-hoc Step-1b instruction), confirm-or-skip (optional, NEVER blocking — phase-12 R6 shape), on confirm run `arete meeting set-area` BEFORE `arete meeting approve` so commitments inherit.
- AC: prose tests — propose-not-auto-write + optional-never-blocking + set-area-before-approve ordering pinned in `chef-orchestrator-skills.test.ts`.

### Slice C — claim tooling

**task-13 — AC5a: `CommitmentsService.setProjectSlug`**
`packages/core/src/services/commitments.ts`: `setProjectSlug(idPrefix, projectSlug | null)` — prefix resolution per existing `resolve()` semantics (0 matches → error; 2+ → error listing matches, no write); runs under the existing lock; `null` clears.
- AC: set, clear, ambiguous-prefix rejection, unknown-id error (unit, mirrors `backfillArea` tests).
- AC: hash-invariance PINNED (review finding 5) — stamping/clearing `projectSlug` leaves `computeCommitmentHash` inputs/ID unchanged (explicit assertion mirroring area-not-in-hash contract).

**task-14 — AC5b: `arete commitments claim` CLI**
`packages/cli/src/commands/commitments.ts`: `claim <id-prefix> --project <slug>` + `--clear` + `--json`. Project slug validated against `projects/active/<slug>/` AND `projects/archive/{<slug>,YYYY-MM_<slug>}/`; unknown → error, no write.
- AC: CLI subprocess — claim/clear/`--json`; unknown project rejected.
- AC: integration — claim a fixture commitment → project brief Open-work moves it from unclaimed-area to project-claimed; sibling brief no longer shows it (a4fdaf7b case).

### Wrap

**task-15 — AC9/AC10/AC11 wrap tier**
Full `npm test` + `npm run typecheck` (all packages). Dark-code audit (every new export has a non-test caller). Rebuild + commit `dist/` for core+cli. `cli-commands.md` + AGENTS.md rebuild; capabilities.json; routability check for `meeting set-area`, `meeting backfill-area`, `commitments claim`. `rollback.md` (AC11: git revert; `--reset` provenance-scoped; `--clear`; hand-delete two keys). Memory entry + MEMORY.md index + LEARNINGS updates. Net-LOC ledger vs ~+685 with variance notes. Ship report (stop at 5.5).
- AC: full suite green; typecheck clean; dark-code audit 0 unexplained; rollback.md present; ledger honest.
