# Execute skill-integration-hooks PRD

## Pi (preferred)

Open the plan in plan mode and use `/build`:

```
/plan open skill-integration-hooks
/build
```

## Manual (fallback)

Execute the skill-integration-hooks PRD. Load the execute-prd skill from `.pi/skills/execute-prd/SKILL.md`. The PRD is at `dev/work/plans/skill-integration-hooks/prd.md` and the task list is at `dev/work/plans/skill-integration-hooks/prd.json`. Run the full workflow: pre-mortem → task execution loop → holistic review.
