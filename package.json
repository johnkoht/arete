{
  "name": "@arete/cli",
  "version": "0.1.0",
  "description": "Areté - Product Management Workspace CLI",
  "type": "module",
  "bin": {
    "arete": "./bin/arete.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "bin/",
    "dist/"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "dev:setup": "bash scripts/dev-setup.sh",
    "build": "npm run build:agents:dev && npm run build:agents:prod && tsc && node scripts/copy-runtime.js",
    "build:legacy": "npm run build:agents:dev && npm run build:agents:prod && tsc && node scripts/copy-runtime.js",
    "build:packages": "tsc -b packages/core packages/cli",
    "build:agents:dev": "tsx scripts/build-agents.ts dev",
    "build:agents:prod": "tsx scripts/build-agents.ts prod",
    "typecheck": "tsc --noEmit",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "start": "node bin/arete.js",
    "test": "tsx --test 'test/**/*.test.ts' 'packages/core/test/**/*.test.ts'",
    "test:js": "tsx --test 'test/**/*.test.ts' 'packages/core/test/**/*.test.ts'",
    "test:py": "python3 -m unittest scripts/integrations/test_utils.py",
    "test:all": "npm run typecheck && npm run test:js && npm run test:py"
  },
  "keywords": [
    "product-management",
    "pm",
    "workspace",
    "cli",
    "cursor"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@arete/core": "workspace:*",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "fs-extra": "^11.2.0",
    "yaml": "^2.4.0",
    "inquirer": "^9.2.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.13.1",
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}